extends layout

include mixins/_storeCard

block content
  .inner
    h2 Top #{topStores.length} stores
    table.table
      thead
        td photo
        td ranking
        td name
        td reviews
        td Average Rating
      each store, i in topStores
        tr
          td: a(href=`/stores/${store.slug}`)
                img(width=200 src=`/uploads/${store.photo || "store.png"}` alt=store.name)
          td #{i + 1}
          td: a(href=`/stores/${store.slug}`)=store.name
          td #{store.reviews.length}
          td #{Math.round(store.averageRating * 10) / 10} / 5

